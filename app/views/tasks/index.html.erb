<h1>My Tasks</h1>

<ul class = "list-unstyled" >
  <% @tasks.each do |task| %>
    <li>
      <%= task.completed ? "☑️" : "🙅" %>
      <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>


<%= link_to "Add a New Task", "/tasks/new" %>
